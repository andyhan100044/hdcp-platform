# HDCP Platform - 部署架构详解

## 🏗️ 完整部署架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          🌐 公网 (Internet)                              │
│                                                                              │
│  用户访问: yourdomain.com                                                     │
└────────────────────┬────────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         🚦 Nginx 反向代理                                   │
│                         (端口: 80/443)                                     │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────┐            │
│  │  • SSL证书 (HTTPS加密)                                         │            │
│  │  • 负载均衡 (多服务器时)                                         │            │
│  │  • 缓存静态文件                                                   │            │
│  │  • Gzip压缩                                                     │            │
│  │  • 安全防护 (WAF)                                              │            │
│  └────────────────────────────────────────────────────────────┘            │
│                                                                              │
│  路由规则:                                                                │
│  • / → 前端 (Next.js) :3000                                               │
│  • /api/* → 后端API (FastAPI) :8000                                       │
│  • /graphql → GraphQL API :8000                                           │
│  • /cms/* → CMS (Strapi) :1337                                            │
└─────────────┬────────────────────────────────────────────────────────────────┘
              │
   ┌──────────┼──────────┐
   │          │          │
   ▼          ▼          ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│  📱    │ │  🔧    │ │  📝    │
│ 前端    │ │  后端   │ │  CMS    │
│ Next.js │ │ FastAPI │ │ Strapi │
│:3000    │ │ :8000  │ │ :1337  │
└────┬────┘ └────┬────┘ └────┬───┘
     │          │          │
     └──────────┼──────────┘
                │
   ┌───────────┼───────────┐
   ▼           ▼           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          💾 数据存储层                                      │
│                                                                              │
│  ┌─────────────────────┐  ┌─────────────────────┐                          │
│  │   PostgreSQL        │  │      Redis         │                          │
│  │   数据库            │  │     缓存           │                          │
│  │   端口: 5432       │  │     端口: 6379    │                          │
│  │   • 存储所有数据     │  │   • 缓存热点数据   │                          │
│  │   • 事务支持         │  │   • 会话存储       │                          │
│  │   • 备份策略         │  │   • 速率限制       │                          │
│  └─────────────────────┘  └─────────────────────┘                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🤔 Nginx 是谁？给谁用？

### Nginx 的角色

**Nginx = 网站的"门卫" + "交通指挥官"**

```
没有 Nginx:
用户 → 直接访问后端 → 暴露内部服务 → 不安全 ❌

有 Nginx:
用户 → Nginx → 根据路径分发 → 各个服务 ✅
```

### ✅ Nginx 的作用

| 功能 | 作用 | 用户受益 |
|------|------|----------|
| **反向代理** | 隐藏真实服务器IP | 更安全 |
| **负载均衡** | 多服务器时分发请求 | 更稳定 |
| **SSL终止** | 处理HTTPS证书 | 更安全 |
| **静态文件缓存** | 缓存CSS/JS/图片 | 更快 |
| **Gzip压缩** | 压缩传输数据 | 更省流量 |
| **安全防护** | 防DDoS、限流 | 更稳定 |

## 🚀 谁需要部署 Nginx？

### 🏠 本地开发环境 (开发阶段)

**如果您只是想在电脑上测试:**

```
方案1: 不使用 Nginx (推荐新手)
用户 → 直接访问 :3000 (前端) ✅ 简单

方案2: 使用 Nginx (推荐专业)
用户 → Nginx (:80) → :3000 (前端) ✅ 接近生产
```

**如何启动?**

```bash
# 启动所有服务 (包括 Nginx)
docker-compose up -d

# 访问地址:
# 有 Nginx: http://localhost
# 无 Nginx: http://localhost:3000
```

### ☁️ 生产环境 (必须使用)

**部署到云服务器时，强烈建议使用 Nginx:**

```
生产部署架构:
┌─────────────────────────┐
│  用户                    │  访问 yourdomain.com
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  云服务器                                   │
│  ┌─────────────────────────────────────┐│
│  │  🌐 Nginx (端口 80/443)             ││  ← 必须使用
│  │  • SSL证书                            ││
│  │  • 负载均衡                          ││
│  │  • 安全防护                          ││
│  └────────────┬────────────────────────┘│
└───────────────┼──────────────────────────┘
                │
      ┌─────────┼─────────┐
      ▼         ▼         ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │ 前端容器│ │ 后端容器│ │ CMS容器 │
   │ :3000  │ │ :8000  │ │ :1337  │
   └────────┘ └────────┘ └────────┘
```

### 何时必须使用 Nginx？

| 场景 | 必须使用 Nginx? | 说明 |
|------|----------------|------|
| **本地测试** | ❌ 不必须 | 可以直接访问 :3000 |
| **单机部署** | ✅ 推荐 | 需要公网访问 |
| **多机部署** | ✅ 必须 | 负载均衡 |
| **生产环境** | ✅ 必须 | SSL、安全 |
| **微服务架构** | ✅ 必须 | 服务治理 |

## 📋 部署方案对比

### 方案1: 简化版 (无 Nginx)

```yaml
# docker-compose.simple.yml
services:
  postgres:
    # 数据库
  redis:
    # 缓存
  api:
    # 后端API
  web:
    # 前端
  cms:
    # CMS
```

**访问地址:**
- 前端: `http://localhost:3000`
- API: `http://localhost:8000`
- CMS: `http://localhost:1337`

**适合:** 本地开发、快速原型

### 方案2: 完整版 (有 Nginx)

```yaml
# docker-compose.yml (包含 Nginx)
services:
  postgres:
  redis:
  api:
  web:
  cms:
  nginx:  # ← 反向代理
    ports:
      - "80:80"
      - "443:443"
```

**访问地址:**
- 前端: `http://localhost` (或 `http://yourdomain.com`)
- API: `http://localhost/api`
- CMS: `http://localhost/cms`

**适合:** 生产环境、公网部署

## 🔧 Nginx 配置详解

```nginx
# nginx/nginx.conf
events {
    worker_connections 1024;
}

http {
    # 上游服务器
    upstream frontend {
        server web:3000;
    }

    upstream backend {
        server api:8000;
    }

    upstream cms {
        server cms:1337;
    }

    # 主服务器配置
    server {
        listen 80;
        server_name localhost yourdomain.com;

        # 前端 (Next.js)
        location / {
            proxy_pass http://frontend;
            # ... 转发到前端
        }

        # API 路由
        location /api/ {
            proxy_pass http://backend;
            # ... 转发到API
        }

        # GraphQL
        location /graphql {
            proxy_pass http://backend;
            # ... 转发到GraphQL
        }

        # CMS
        location /cms/ {
            proxy_pass http://cms/;
            # ... 转发到CMS
        }
    }
}
```

## 🎯 不同环境的部署建议

### 1️⃣ 本地开发 (个人电脑)

**推荐: 使用简化版 (无 Nginx)**

```bash
# 启动简化版
docker-compose -f docker-compose.simple.yml up -d

# 访问
http://localhost:3000  # 前端
http://localhost:8000  # API
```

**优点:**
- ✅ 简单、快速
- ✅ 调试方便
- ✅ 不需要额外配置

**缺点:**
- ❌ 端口多 (3000, 8000, 1337)
- ❌ 没有HTTPS
- ❌ 没有负载均衡

### 2️⃣ 测试环境 (云服务器)

**推荐: 使用完整版 (有 Nginx)**

```bash
# 启动完整版
docker-compose up -d

# 访问
http://your-server-ip  # 前端
http://your-server-ip/api  # API
http://your-server-ip/cms  # CMS
```

**优点:**
- ✅ 统一入口 (80端口)
- ✅ 接近生产环境
- ✅ 可以配置域名

**缺点:**
- ❌ 稍复杂

### 3️⃣ 生产环境 (公网)

**必须: 使用完整版 + SSL**

```bash
# 配置域名和 SSL 证书
# 然后启动
docker-compose up -d

# 访问
https://yourdomain.com  # 前端 (HTTPS)
https://yourdomain.com/api  # API (HTTPS)
https://yourdomain.com/cms  # CMS (HTTPS)
```

**优点:**
- ✅ 安全 (HTTPS)
- ✅ 性能优化
- ✅ 稳定可靠

## 🔍 常见问题

### Q1: 我只想测试，需要配置 Nginx 吗？

**答:** 不需要！直接用简化版即可。

```bash
# 修改 docker-compose.yml，注释掉 nginx 服务
# services:
#   nginx:
#     # 注释掉
```

### Q2: 我部署到云服务器，应该用 Nginx 吗？

**答:** 必须用！

```bash
# 使用完整版 docker-compose.yml
# 配置域名和 SSL
docker-compose up -d
```

### Q3: Nginx 会影响性能吗？

**答:** 不会！Nginx 反而会提升性能。

- **缓存静态文件**: 减少后端压力
- **Gzip压缩**: 减少传输量
- **负载均衡**: 提升并发能力

### Q4: 端口被占用怎么办？

**答:** 修改 docker-compose.yml

```yaml
services:
  nginx:
    ports:
      - "8080:80"  # 改为 8080
```

## 📊 部署决策树

```
开始
  │
  ├─ 本地开发?
  │    └─ 是 → 使用简化版 (无 Nginx) ✅
  │
  ├─ 云服务器测试?
  │    └─ 是 → 使用完整版 (有 Nginx) ✅
  │
  └─ 生产环境?
       └─ 是 → 使用完整版 + SSL + 监控 ✅
```

## 🎉 总结

### Nginx = 生产环境必备

| 环境 | 使用 Nginx? | 原因 |
|------|-------------|------|
| **本地开发** | ❌ 可选 | 快速调试 |
| **测试环境** | ✅ 推荐 | 接近生产 |
| **生产环境** | ✅ 必须 | 安全+性能 |

### 🚀 推荐操作

**新手 (仅测试):**
```bash
# 简化版
docker-compose -f simple.yml up -d
# 访问: http://localhost:3000
```

**专业 (生产部署):**
```bash
# 完整版
docker-compose up -d
# 配置域名和 SSL
# 访问: https://yourdomain.com
```

---

**记住**: Nginx 不是负担，是必需品！在生产环境中，它是安全、性能、稳定的保障。
